/*
export const pdfImpresion = (tipo, data = []) => {
    let result;
    switch (tipo) {
        case "alta": result = imprimirAlta(data); break;
        case "hojaRuta": result = imprimirHojaRuta(data); break;
    }

    return result
}

const imprimirAlta = (data = []) => {}

*/